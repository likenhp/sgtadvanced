
const creds = {
    host     : 'host',
    user     : 'user',
    password : 'password',
    database : 'database',
    port: port
}

module.exports = creds; //node thing, when I require, that is what I will give it

